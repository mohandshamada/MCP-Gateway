# MCP Gateway Secrets Configuration
# Copy this file to secrets.env and fill in your values
# NEVER commit secrets.env to version control!

# Gateway Authentication Tokens
# Comma-separated list of valid API tokens for accessing the gateway
# Clients must include: Authorization: Bearer <token>
MCP_GATEWAY_TOKENS=sk-your-secret-token-here,sk-another-token-if-needed

# Single token alternative (if you only need one)
# MCP_GATEWAY_TOKEN=sk-your-secret-token

# GitHub Personal Access Token (for GitHub MCP server)
# Required scopes: repo, read:org (optional)
GITHUB_TOKEN=ghp_your_github_token_here

# Brave Search API Key (for Brave Search MCP server)
BRAVE_API_KEY=BSA_your_brave_api_key

# Database Connection Strings
# PostgreSQL
POSTGRES_URL=postgresql://user:password@localhost:5432/dbname

# Additional environment variables for sub-servers
# Add any server-specific environment variables here
